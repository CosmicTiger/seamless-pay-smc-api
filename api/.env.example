# Local hardhat node
RPC_HTTP_LOCAL=http://127.0.0.1:8545
# If you need writes, add a funded private key from the hardhat node:
PRIVATE_KEY=0xabc123...deadbeef
# Path to your contracts project (used to read artifacts/ignition)
CONTRACTS_DIR=../contracts

# Server settings
PORT=3000
NODE_ENV=development
